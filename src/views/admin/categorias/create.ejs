<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Categorias</title>
    <link rel="stylesheet" href="/css/index.css">
    
 
</head>

<body>
    <%- include('../../partials/header') %>
    <form 
    action="/admin/categorias" 
    method="post"
    >

        <main class="main">
          <h1 class="crear-item">CREAR CATEGORIA</h1>

          <% if (typeof errors != 'undefined') { %>
            <ul>
            <% errors.forEach(error => { %>
            <li><%= error.msg %></li>
            <% }) %>
            </ul>
            <% } %>
  
        <div class="contenedor-div">
       
            <form action="/admin/categorias" method="post">
                <div>
                  <label class="subtitulos" for="name">Nombre de la licencia </label>
                  <input
                    type="text"
                    id="name"
                    name="name"
                    value="<%= typeof values != 'undefined' ? values.name : '' %>"
                  />
                </div>
                <button type="submit">Guardar</button>
              </form>
            </main>
            <%- include('../../partials/footer') %>
          </body>
        </html>
     
   
        
  
       
      
<!--    
          <div class="botones">
              <button class="boton" type="submit">Agregar categorias</button>
              <button class="boton">Limpiar</button>
          </div> -->
               
      <!-- </main>
    </form>
        <%- include('../../partials/footer') %>
</body>

</html> -->